package com.myteam.swimmingpool;

import java.lang.Number;

rule "CalcAgeFromCurrentDate"
	dialect "mvel"
	when
		exists (pp : Person( bdate : birthDate != null , age == null ) and Quote( curDate : quoteDate != null )) 
	then
end
