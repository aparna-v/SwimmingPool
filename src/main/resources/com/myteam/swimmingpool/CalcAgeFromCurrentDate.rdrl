package com.myteam.swimmingpool;

import java.lang.Number;

rule "CalcAgeFromCurrentDate"
	dialect "mvel"
	when
		exists (Person( bdate : birthDate != null , age == null ) and Quote( curDate : quoteDate != null )) 
	then
end
