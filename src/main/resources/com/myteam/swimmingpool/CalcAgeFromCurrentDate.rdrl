package com.myteam.swimmingpool;

import java.lang.Number;

rule "CalcAgeFromCurrentDate"
	dialect "mvel"
	when
		exists (Person( birthDate != null , age == null ) and Quote( quoteDate != null )) 
	then
end
