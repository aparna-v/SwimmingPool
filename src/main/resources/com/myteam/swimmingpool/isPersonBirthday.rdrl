package com.myteam.swimmingpool;

import java.lang.Number;

rule "isPersonBirthday"
	dialect "mvel"
	when
		pper : Person( birthDate != null )
		qd : Quote( quoteDate != null )
		not (cc : CalculatedAttribute( key == "IsPersonBirthday" ) and Person( )) 
	then
end
